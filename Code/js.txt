js 脚本语言
js组成：
·ECMAScript语言标准
·DOM文档模板对象
·BOM浏览器模板对象
特点：
·语法与Java类似
·解释性语言 边执行 边解释
·向HTML页面中添加交互行为


数据类型
·undefined 变量只是声明没有赋值
·null 一个空值与undefined值相等
·number 整数 浮点数
·bollean 布尔值
·string 单引号 或者双引号引起来的文本
css8.cn

getElementById();对象id的第一个对象的引用   对象id唯一
getelementsByName 返回带指定名称的对象的集合 相同name属性
getElementsByTagName() 返回带有指定签名的对象集合   相同的元素



getTime()  返回从1970 1 1以来的毫秒数
 var inum=Math.floor(Math.random()*98+2);

setTimeout("调用的函数"，等待的毫秒数);

getAttribute("属性名");获取属性值
setAttribute("属性名"，"属性值")设置属性值

原型对象
每个函数都有一个prototype属性，这个属性是一个指针，指向一个对象
prototype就是通过调用构造函数而创建的那个对象实例的原型对象

一个原型对象谁另一个原型对象的实例
相关的原型对象层层递进，就构成了实例与原型的链条，就是原型链